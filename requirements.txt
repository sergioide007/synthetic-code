# Instalación mínima (sin APIs reales, sólo patrones + demos con MockProvider):
#   pip install -e .
#
# Instalación completa (recomendada para seguir el libro):
#   pip install -e ".[all]"
#
# Este fichero se mantiene por compatibilidad con `pip install -r requirements.txt`;
# la fuente de verdad de las dependencias es pyproject.toml.
anthropic>=0.40
openai>=1.50
requests>=2.31
redis>=5.0
jsonschema>=4.20
pytest>=8.0
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
